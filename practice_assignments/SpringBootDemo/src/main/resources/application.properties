# This must be added to any project with tomcat
# Tomcat configuration
server.servlet.contextPath=/
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# max file size on file upload in bytes
fileupload.maxFileSize=5242880

# Mysql Database Setup
spring.jpa.database=mysql
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.flyway.enabled=false

# Mysql Database Setup
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/perscholas?useLegacyDatetimeCode=false&autoReconnect=true&useSSL=false&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=C1nder666

# Logging
logging.level.org.springframework=INFO
logging.level.org.perscholas=DEBUG

